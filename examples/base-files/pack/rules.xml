<?xml version="1.0" encoding="utf-8"?>
<rules>

    <install>
    <![CDATA[

cp -a $XPACK_SOURCE_DIR/* $XPACK_INSTALL_DIR 

case "$(uname -m)" in
    x86_64)
        ln -sv lib $XPACK_INSTALL_DIR/lib64
        ln -sv lib $XPACK_INSTALL_DIR/usr/lib64
    ;;
esac

    ]]>
    </install>

    <clean>
    <![CDATA[

rm -fr $XPACK_INSTALL_DIR

    ]]>
    </clean>

</rules>
